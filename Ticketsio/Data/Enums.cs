namespace Ticketsio.Data
{
    public enum MovieStatus
    {
        UpComing,
        Available,
        Expired
    }
    public enum TicketStatus
    {
        Pending,
        Paid,
        Canceled,
        Refunded
    }
}
